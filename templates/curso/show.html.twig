{% extends 'base.html.twig' %}

{% block title %}Curso{% endblock %}

{% block body %}
    <h1>Curso</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCurso</th>
                <td>{{ curso.idCurso }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ curso.nombre }}</td>
            </tr>
            <tr>
                <th>FechaPublicacion</th>
                <td>{{ curso.fechaPublicacion ? curso.fechaPublicacion|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ curso.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_curso_index') }}">back to list</a>

    <a href="{{ path('app_curso_edit', {'idCurso': curso.idCurso}) }}">edit</a>

    {{ include('curso/_delete_form.html.twig') }}
{% endblock %}
